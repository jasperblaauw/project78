package nl.hr.server;

public class PersonMover {
    // This class takes the chokepoint id and person id to move people to different
    // rooms when they pass a chokepoint

    public void processChokepointData(int chokepointId, int personId) {
        System.out.println(chokepointId + " " + personId);
    }

}
